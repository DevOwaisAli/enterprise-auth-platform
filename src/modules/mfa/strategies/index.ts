/**
 * MFA module does not register additional Passport strategies — the TOTP and
 * backup-code verification flows are handled by MfaLoginService.
 * This file exists to keep the module structure consistent.
 */
export {};
